import { LegalPage } from "@/components/LegalPage";

export default function SupportPage() {
  return <LegalPage title="Support" body="Report a problem at support@andromeda.local. Before public launch, replace this with your real support inbox and add the same email to the footer and Resend domain." />;
}
